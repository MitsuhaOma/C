#include<stdio.h>
 int main(void) 
 {
 	int no,i,j;
 	puts("让我们画一个向下的金字塔。");
	 printf("金字塔有几层：");scanf("%d",&no) ;
	 for(i=1;i<=no;i++){
	 for(j=1;j<i;j++)
	  putchar(' ');
	  for(j=0;j<(2*(no-i)+1);j++)
	  printf("%d",i) ;
	  putchar('\n'); 
	 }
	 
 	return 0;
 }
