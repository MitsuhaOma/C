#include<stdio.h>
 int main(void) 
 {
 	int no,i,j;
 	puts("生成直角在左上方的直角三角形。");
	 printf("短边：");scanf("%d",&no) ;
	 for(i=0;i<no;i++){
	 	for(j=0;j<(no-i);j++)
	 	  putchar('*');
	 	  putchar('\n');
	 }
	 
 	return 0;
 }
