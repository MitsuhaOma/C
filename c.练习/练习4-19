#include<stdio.h>
 int main(void) 
 {
 	int no,i,count;
 	printf("整数值:");
 	scanf("%d",&no);
 	count=0;
 	for(i=1;i<=no;i++){
if (no%i==0){
   printf("%d\n",i);
   count++;
   }
   	}
   	printf("%d的约数有%d个。\n",no,count);
 	return 0;
 }
