#include<stdio.h>
 int main(void) 
 {
 	int no,i,j;
 	puts("让我们画一个金字塔。");
	 printf("金字塔有几层：");scanf("%d",&no) ;
	 for(i=1;i<=no;i++){
	 for(j=0;j<(no-i);j++)
	  putchar(' ');
	  for(j=0;j<((i-1)*2+1);j++)
	  putchar('*');
	  putchar('\n'); 
	 }
	 
 	return 0;
 }
