#include<stdio.h>
 int main(void)
 {
 	int no,i,t;
 	printf("请输入一个正整数：");
	 scanf("%d",&no);
	 t=no/2;
	 i=1;
	 if (no>0)  {
	    if (no%2==1)
	    while(i<=t){
	 	printf("%d ",i*2);
	 	i++;
	    } 
	 else   
	 while(i<t){
	 	printf("%d ",i*2);
	 	i++;
		 }
	printf("\n");}
	 return 0 ;
  } 
