#include<stdio.h>
int main(void)
{
	int no,i,gin;
	i=0;
	
	do{
	printf("请输入一个正整数:");
	scanf("%d",&no) ;
	if(no<=0)
	 puts("\a请不要输入非正整数。");
 }while(no<=0);
 	gin=no;
 	while(no>0){
 		no/=10;
 		i++; 
	 }
	printf("%d的位数为%d。\n",gin,i);
 return 0;
 } 
